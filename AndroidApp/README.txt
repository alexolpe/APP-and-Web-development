A la carpeta java s'hi troben els paquets com.example.coursemanager i getRequest que permeten el funcionament de la app

A la carpeta res s'hi troba el paquet layout on hi ha definit el disseny de l'app